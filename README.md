# Build the app 

```bash
tinygo build -target=wasi -o main.wasm main.go
```

# Copy to the node folder the wasm file
```bash
cp main.wasm ../
```

# Move out one folder
```bash
cp main.wasm ../
```

# Run it

```bash
node index.js
```
